using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class QuestTextItem : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log($"quest text item instantiated at position {gameObject.transform.position}");
    }
}
